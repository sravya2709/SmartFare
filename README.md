# SmartFare
SmartFare is a travel cost optimization system that finds the minimum-cost route between two cities using Greedy and Dynamic Programming algorithms. It compares both approaches, displays the best path, and shows which algorithm provides the optimal solution, demonstrating practical graph-based optimization.
